name: vet_smart
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  file_picker: ^6.1.1
  fl_chart: ^0.64.0
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  quickalert: ^1.0.2
  syncfusion_flutter_pdf: ^23.1.44

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

assets:
  - assets/
